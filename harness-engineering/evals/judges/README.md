# judges/ — LLM 저지

저지 모델은 **평가 대상과 다른 모델**을 쓴다 (`config/models.yaml`의 `judge`).
같은 모델로 채점하면 자기 편향을 그대로 통과시킨다.

## 저지를 믿기 전에 저지를 평가한다

주당 20건을 사람이 병행 채점하고 상관계수를 본다. 0.7 아래면 저지가 아니라
**루브릭**을 고친다. 대개 기준이 모호한 것이 원인이다.

상관계수 추이는 `reports/judge-calibration.md`에 기록한다.
