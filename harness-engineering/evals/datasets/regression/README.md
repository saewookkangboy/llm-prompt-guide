# regression/ — 회귀 케이스

파일명은 `<발생일>-<증상>.jsonl`. 케이스마다 다음을 기록한다.

- `origin_trace`: 어떤 실행에서 발견됐는가
- `root_cause`: 무엇 때문이었는가
- `fixed_in`: 어느 PR/커밋에서 고쳤는가

원인을 적지 않으면 6개월 뒤에 이 케이스가 왜 있는지 아무도 모르고,
결국 "이상해 보인다"는 이유로 삭제된다.
